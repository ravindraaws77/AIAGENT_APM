python-dotenv>=1.0
pytest>=8.0
pytest-asyncio>=0.24  # for tests/test_ui_pages.py's use of nicegui.testing's async `user` fixture

# Phase 2: Gmail (and shared Google OAuth helper used by phase 3's
# Calendar connector too).
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Phase 4: MS Excel via Microsoft Graph.
msal>=1.28
requests>=2.31

# Phase 5: LangGraph agent + Claude reasoning.
langgraph>=0.2
anthropic>=0.40

# Phase 6a: FastAPI backend.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27

# Phase 6b: NiceGUI frontend.
nicegui>=1.4

# Local/Google Drive Excel connector: parses/edits .xlsx bytes directly
# (no cell-range API on either source, unlike Graph's Excel API). Drive
# access itself reuses google-api-python-client above.
openpyxl>=3.1
