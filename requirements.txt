python-dotenv>=1.0
pytest>=8.0

# Phase 2: Gmail (and shared Google OAuth helper used by phase 3's
# Calendar connector too).
google-api-python-client>=2.100
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Remaining tool-specific and agent/UI dependencies (msgraph-sdk,
# langgraph, langchain-anthropic, streamlit, ...) are added in each
# later phase's own PR — see docs/roadmap.md.
