# Capability map — Workstream 2 (Agent Tools & Connectivity)

Deliverable per the team's workstream brief: *"Capability map by
system/tool, read/write/action possibilities, recommended connection
paths, known gaps/constraints/blockers."*

## Orchestration/automation platform comparison

Evaluated per the brief's mission ("explore OpenAI, Anthropic, and other
viable platforms; check connectors, plugins, MCPs, APIs, and available
tooling"):

| Platform | Type | Read/observe | Write/act | State + HITL approval | Verdict |
|---|---|---|---|---|---|
| **LangGraph** | Code framework (Python) | Yes, via any tool/MCP client | Yes, via any tool/MCP client | Native: checkpointer for persistent state, `interrupt()` / `Command(resume=...)` for human approval gates | **Chosen for the orchestration layer — implemented in phase 5** (`src/apm/agent/graph.py`). Matches the master architecture's Layer 4. Approval logic lives in reviewable code, not a visual canvas. One real gotcha found and fixed while implementing: code placed *before* an `interrupt()` call inside a node re-executes on resume, so side effects (like recording a pending action) must live in a separate, non-interrupting node — see the `propose_node`/`approval_node` split and its docstrings. |
| **MCP (Model Context Protocol)** | Connectivity standard | Yes | Yes | N/A (a transport/tooling standard, not an orchestrator) | **Chosen for the tool-connectivity layer** (Layer 5). Anthropic-originated, now an open standard; both our master architecture diagram and this workstream's own "suggested tools" call it out directly. |
| n8n (self-hosted, 2.0) | Low-code workflow platform | Yes, ~70 AI nodes incl. LangChain integration | Yes | Built-in persistent memory + HITL node patterns | Strong alternative; deepest AI-agent features among no-code tools and cheapest at volume (per-execution, not per-step pricing). Good candidate for a *later* notification/glue channel, not the core reasoning/approval brain for this challenge. |
| Make | Low-code workflow platform | Yes | Yes | Beta agent builder ("Maia") | Best visual-builder / cost balance of the no-code options; same "logic isn't in reviewable code" limitation as n8n for our purposes. |
| Zapier | SaaS automation, 8,000+ app connectors | Yes | Yes | Zapier Agents (higher-level, less configurable) | Widest connector catalog and the simplest non-technical setup; per-step task pricing is expensive at volume. Already available as an MCP-exposed connector in this environment — worth keeping in mind as a fast path for a connector we haven't built ourselves yet. |

**Decision:** LangGraph orchestrates; tools are exposed as MCP-style
connectors underneath it. No-code platforms are documented here as the
comparison this workstream asked for, not built into the MVP.

## Per-tool capability assessment

| Tool | Auth | Read/retrieve | Write/action | Recommended connection path | Known gaps / blockers |
|---|---|---|---|---|---|
| **Gmail** | Google OAuth 2.0 (user consent) | **Implemented (phase 2):** search/list threads, read message content and metadata — `src/apm/tools/gmail_tool.py` | **Implemented (phase 5):** send an email (`send_email`) — only ever called with `dry_run=False` from inside the agent graph's `execute_node`, after an approved `interrupt()`; draft/apply-labels remain unimplemented | Google API Python client (`google-api-python-client`) wrapped as a typed tool (`GmailTool`) with explicit capability flags; a `GmailClient` protocol lets tests substitute a fake client with no live credentials; can be re-exposed as an MCP server later without changing the tool interface | Sending is irreversible — always gated behind approval, never called with `dry_run=False` outside the graph. Needs a Google Cloud OAuth client (one-time setup, credentials never committed — see `.env.example`). Requests both `gmail.readonly` and `gmail.send` scopes (least-privilege beyond that: no broader Gmail scope requested). |
| **Google Calendar** | Google OAuth 2.0 (same consent screen as Gmail, can share scopes) | **Implemented (phase 3):** list/search events, read attendees/times/location — `src/apm/tools/calendar_tool.py` | **Implemented (phase 5):** create a single event (`create_event`) — same approval-gated pattern as Gmail's send; update-event/respond-to-invite remain unimplemented | Same Google API client library as Gmail (`GoogleApiCalendarClient`, same `google_auth.py` helper); a `CalendarClient` protocol lets tests substitute a fake client with no live credentials | Time zone / recurring-event handling adds complexity — out of scope for MVP (single, non-recurring events only). The Gmail/Calendar shared-token-cache issue is resolved: `apm.tools.google_auth.build_gmail_and_calendar_tools` requests both tools' scopes in one consent, used by the agent (`scripts/agent_demo.py`); the single-tool `build_gmail_tool`/`build_calendar_tool` factories still exist for the phase 2/3 demo scripts, which use one tool at a time. |
| **Excel files (local + Google Drive)** — **the default Excel connector** | Local: none. Google Drive: Google OAuth 2.0, full `drive` scope (same client as Gmail/Calendar, separate token cache) | **Implemented:** list worksheets, read a cell range — `src/apm/tools/excel_file_tool.py`. Wired into the agent graph's `fetch_node` (`src/apm/agent/graph.py`), same as `ms_excel` | **Implemented:** overwrite a cell range (`write_range`) — same dry-run-gated pattern as Gmail/Calendar/MS Excel; reachable from `execute_node` under the key `"excel_file"`, only ever called with `dry_run=False` after an approved `interrupt()` — see `tests/test_agent_graph.py`'s renewal-tracker business-scenario tests for the full fetch → propose → approve/reject → execute cycle. Verified live against a real Google Drive file (read and write, with a reject-then-approve pass via `scripts/excel_file_write_demo.py`) | Complements the Graph-based MS Excel connector below for workbooks that are plain `.xlsx` files rather than something already living on OneDrive/SharePoint: a `WorkbookSource` protocol (`LocalWorkbookSource` for a path on disk, `GoogleDriveWorkbookSource` downloading/uploading file bytes via the Drive v3 API) supplies whole-workbook bytes, which `ExcelFileTool` parses/edits with `openpyxl` — there's no cell-range API on either source, unlike Graph's Excel API. Same `ExcelClient`-shaped test-fake pattern (`FakeWorkbookSource`) as the other connectors. **This is the only Excel connector `apm.api.dependencies.get_graph` actually builds** — a deliberate choice, not an accident: MS Excel (below) needs a separate Azure AD app registration most deployments won't have set up, while local/Drive workbooks need either nothing (local) or the same Google OAuth client Gmail/Calendar already use (Drive). `apm.tools.excel_file_tool.build_configured_excel_tool` builds the one workbook (`APM_EXCEL_WORKBOOK_PATH` local path, or `APM_EXCEL_DRIVE_FILE_ID` — a Drive file id *or* name, resolved via the shared `resolve_drive_file_id` — at most one, same "the account" spirit as Gmail/Calendar) the running server is configured for, added to `apm.api.dependencies.get_graph`'s tools when set. **Found by live testing:** the first version of this required a literal Drive file id in `APM_EXCEL_DRIVE_FILE_ID` — a user set it to the file's name instead (the CLI script already accepted a name, so this was the reasonable thing to try) and got a Drive 404 for a "file" whose id was literally the string `"APM_Invoice_Details.xlsx"`. `resolve_drive_file_id` (originally only in `scripts/excel_file_demo.py`) moved into `excel_file_tool.py` so both the CLI and `build_configured_excel_tool` resolve a name the same way — raising loudly (not guessing) if it matches zero or more than one file, since a wrong silent guess here would misconfigure every request on a running server, not just one script invocation. Reached from the dashboard's free-text "Ask APM" box: `apm.agent.intent`'s `ParsedIntent.excel_query` (a yes/no signal, not a sheet/address choice — the intent parser has no visibility into the workbook's layout) tells `/query` to fetch the workbook's default range (first sheet, whole used range, per `read_range`'s own defaults below); the approval card already had a `write_range` case in `apm.ui.logic.format_action_details` (Sheet/Range/Rows) before this, so no UI changes were needed there. See `tests/test_ui_pages.py`'s `test_excel_renewal_tracker_approval_flow_through_the_ui` for the real end-to-end proof. **Found by live testing:** a real "update order 223's status to Paid" request went nowhere — two independent gaps, both from `apm.agent.reasoner.SYSTEM_PROMPT` and `apm.agent.intent.SYSTEM_PROMPT` never being updated when `excel_file` was wired in. (1) The reasoner's tool enum only listed `"ms_excel"`, so it could never legally propose a `write_range` action against the connector actually configured (`"excel_file"`) — fixed by adding `"excel_file"` to the enum and instructing it to match whichever tool key the spreadsheet data appeared under in the fetched context, rather than guessing. (2) The intent parser's `excel_query` guidance only gave "check the tracker for X"-style read examples, biasing it away from recognizing an update-phrased request ("update X's status to Y", "mark X as done") as spreadsheet-relevant — broadened with explicit write-phrased examples. Both prompts are plain strings sent to Claude, invisible to type checking, so regression-guarded with a literal content assertion (`tests/test_reasoner.py::test_system_prompt_lists_excel_file_as_a_proposable_tool`, `tests/test_intent.py::test_system_prompt_covers_write_phrased_excel_requests`) rather than relying on prompt-engineering alone. | Local source needs no credentials at all. Drive source only handles an actual `.xlsx` uploaded to Drive, not a native Google Sheet (Sheets is a different file type with no `.xlsx` bytes to download — would need the separate Sheets v4 API, out of scope here) — bound to one Drive file id at construction time, same as MS Excel's workbook binding. `read_range` loads with `data_only=True` to return each formula cell's last-*calculated* value; a workbook that's never been opened in real Excel/Sheets (e.g. one only ever written by openpyxl) has no cached value, so such cells read back as `None` — not a concern for `write_range`, which reloads without `data_only` so it never clobbers other cells' formulas when saving. **Found by live testing:** the initially-chosen narrow `drive.file` scope (least-privilege, only files this app created/picked) turned out unusable for the connector's real use case — writing to an existing file the user just points it at by id/name returned a 403 `insufficientPermissions`; reads happened to succeed anyway, which only made the gap more confusing until write was actually exercised. Widened to the full `drive` scope (see `excel_file_tool.py`'s module docstring) since there's no Picker-based incremental-auth flow implemented here. `scripts/agent_demo.py` still only drives Gmail + Calendar by default (unchanged; the API/UI is the wired path now), same gap as MS Excel below. |
| **MS Excel** — *optional, not currently wired into the API/UI* | Microsoft identity platform (Azure AD app registration), MSAL device-code flow, Microsoft Graph API | **Implemented (phase 4):** list worksheets, read a cell range — `src/apm/tools/excel_tool.py` | **Implemented (phase 5):** overwrite a cell range (`write_range`) — same approval-gated pattern as Gmail/Calendar; append-to-table remains unimplemented | Plain `requests` against the Graph REST API (`GraphApiExcelClient`), wrapped with the same tool interface as Gmail/Calendar; an `ExcelClient` protocol lets tests substitute a fake client with no live credentials | Needs a separate Azure AD app registration (different admin/consent flow than Google) — this is the reason Excel is its own phase rather than bundled with the Google tools, and the reason "Excel files" above (not this) is the default: onboarding OneDrive/SharePoint support means a second identity-provider setup most deployments won't need. Workbook must already be on OneDrive/SharePoint (Graph API doesn't operate on local `.xlsx` files). Unlike Gmail/Calendar, the connector is bound to one specific workbook (drive item id) at construction time rather than operating on "the" mailbox/calendar — `scripts/excel_demo.py --list` helps find that id. **Not wired into `apm.api.dependencies.get_graph`** (or `scripts/agent_demo.py`) — `fetch_node`/`execute_node` (`src/apm/agent/graph.py`) already handle an `"ms_excel"` tool key generically, and the reasoner's prompt (`apm.agent.reasoner.SYSTEM_PROMPT`) is written to match whichever tool key the spreadsheet data appears under rather than hard-coding one (see the `excel_file` tool-name prompt fix), so onboarding this later is additive: a `build_configured_ms_excel_tool`-equivalent factory (mirroring `excel_file_tool.build_configured_excel_tool`) plus a couple of env vars in `dependencies.py`, no changes needed elsewhere. |

## Common tool interface

Every connector implements the same shape (see `src/apm/tools/base.py`) so
the orchestration layer and the UI don't need tool-specific code:

- `capabilities`: which of `read`, `write`, `action` this tool supports
- `dry_run`: when true, a write/action call returns what *would* happen
  without doing it — used for local testing without live credentials
- every call is written to the audit log in `src/apm/state/store.py`,
  whether it was a read, a proposed write, an approval, a rejection, or an
  executed action
